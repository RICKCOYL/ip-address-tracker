import React from 'react';
import mapImg from '../assests/images/googleimg.png';

const Maps = () => {
    return (
        <div id="map-div">
            <img id='map' src={mapImg} alt="" />
        </div>
    );
};

export default Maps;
